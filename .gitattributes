# 统一行尾与二进制识别（Windows 开发环境，仓库内文本统一 LF）
* text=auto

# 文本文件强制 LF（避免 CRLF 混用导致的 diff 噪音与告警）
*.py   text eol=lf
*.md   text eol=lf
*.sql  text eol=lf
*.json text eol=lf
*.txt  text eol=lf

# 二进制文件不参与行尾转换
*.png  binary
*.jpg  binary
*.db   binary
*.pyc  binary
*.bin  binary
