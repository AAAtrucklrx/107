# 接手日志（滚动会话状态快照）

> 用途：会话压缩/恢复的持久锚点。**上下文接近 300K 时，先刷新本文件再继续工作**（用户约定 2026-08-14）。

## 授权与节奏约定

- 完全接手开发：指挥中心已暂停，本会话独家驱动。
- 顺序：Phase 0（已完成）→ Phase 1 功能 → Phase 2 架构债。
- 垃圾清理：归档不直删（scripts/archive/、docs/archive/，已 gitignore）。
- 提交流程：按里程碑批量 → 在聊天中列变更清单 + 验证结果 → 用户批准 → **用 pwsh 执行 `git commit`**（系统 git_commit 工具被沙箱阻断：无法建 `.git/index.lock`，勿再尝试）。
- 提交身份：`git -c user.name="xiaowo-dev" -c user.email="xiaowo@local" commit`。

## 目标状态

- goal id：`goal-09ba9f74-753c-4d4a-abcd-c6458858de03`（完全接手目标，armed）
- 环境：Python 完整路径 `C:\Users\Richelieu\AppData\Local\Programs\Python\Python314\python.exe`（勿用 `python` 占位符）；PowerShell 无 `&&`；内联 `-c` 引号易炸 → 写脚本文件运行。
- 沙箱注意：`XIAOWO_TEST_TMP=F:\小蜗\scripts\data\tmp_test` 可让 test_fixes 在受限环境跑通（现在文件策略已 full-access，可能不再需要，但保留兼容）。
- 残留：python PID 4360（指挥中心遗留测试实例，未监听端口，勿随意杀）；`scripts/data/tmp_run/`（探针目录，删不掉，已 gitignore）。

## 已完成（git 历史）

| 提交 | 内容 |
|---|---|
| `19a8c6c` | （接手前）代码审查10项修复 |
| `c997e82` | Phase 0 基线稳定：langgraph 依赖、.gitattributes、垃圾归档、crawl 数据移出 git、测试沉淀、硬编码画像修复、学习报告+精读摘要 |

## 当前进行：Phase 1a 选课推荐个性化增强 ✅ 已实现并验证，待提交

已实现（未提交）：
- `tools/advisor_tools.py`：`_TAKEN_INTEREST_KW`、`_infer_preference`（GPA≥3.7 硬核 / ≤2.7 冲分 / 其余均衡，显式偏好优先）、`_generate_reason` 加 GPA 画像理由、`recommend_courses` 加 `gpa` 参数 + 自动画像（`_auto_pref`）+ 已修课程兴趣推断（仅理由信号，不滤池）+ `profile_note` 带 auto/gpa。
- `agents/qa/nodes.py`：`_load_gpa` 新增（本地成绩表算 4.3 制 GPA）；`_enrich_recommend_args` 注入 gpa；`_build_tool_summary` 推荐分支输出画像说明；THINK_PROMPT 规则 10 与 `_TOOL_LIST` 同步更新。
- `scripts/test_fixes.py`：+5 断言（28/28）；`scripts/verify_nodes.py`：+1 断言（5/5）。
- 验证：py_compile ✅ · verify_nodes 5/5 ✅ · test_fixes 28/28 ✅ · verify_tools 31/31 ✅。

**下一步**：
1. 提交里程碑 2（Phase 1a，含 docs/接手日志.md、docs/pip-freeze-2026-08-14.txt）。
2. Phase 1b：培养方案页 × 选课顾问联动（点击方案课程发起推荐/对比）。

## 待办总览（见 todo 板）

- Phase 1a（进行中，如上）
- Phase 1b 前端联动；Phase 1c 方案数据刷新（需 jw 登录态，与用户确认）
- Phase 2a 多用户会话隔离（P1-1）；Phase 2b legacy 退役 + `_resolve_program` 收敛；Phase 2c LLM 输出加固

## 知识锚点

- 学习基线：`docs/学习报告.md`（架构/模块/风险分级/路线图，基线 19a8c6c）
- 精读底稿：`docs/精读摘要_tools_services.md`、`docs/精读摘要_knowledge_ui_database.md`
- 验证套件：check_course_db(9) / verify_tools(31) / test_fixes(23) / verify_nodes(4) / advisor_smoke+browser_e2e+qa_eval(需 LLM)
- 依赖冻结：`docs/pip-freeze-2026-08-14.txt`
