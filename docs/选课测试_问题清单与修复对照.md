# 小蜗选课能力 · 六步迭代测试与调试对照记录（2026-08-15）

> 实例：app_test.py（模拟登录 PB25111691，本地数据）。回答原文逐轮存档：`docs/选课测试_轮{N}_回答原文.md`。
> 硬性要求遵守：所有引用均来自当轮实际页面抓取原文，无编造。

## 测试问题集（7 问）

Q1 按专业与兴趣推荐 / Q2 低workload筛选 / Q3 课表时间冲突 / Q4 学分与培养方案匹配 / Q5 先修课是否满足 / Q6 退补选建议 / Q7 同课多师对比

## 第 1 轮：问题清单（8 组根因，详见轮1评估）

| 组 | 问题 | 原文依据（摘） |
|---|---|---|
| A | 选修组未按学期过滤，学期标注矛盾（"大二下学期（方案学期3春）"） | 轮1 Q1/Q2 |
| B | 选课数据缺 time/学分字段（"缓存中暂无具体课程时间数据"） | 轮1 Q3/Q6 |
| C | 来源声明错误（缓存数据称"实时查询"） | 轮1 Q4 |
| D | 先修课满足性编造且自相矛盾（"概统已修" vs Q4 缺口） | 轮1 Q5 |
| E | 垃圾评论污染（"pksq崩了"） | 轮1 Q1 |
| F | 必修组被重排/称"备选"、需求冲突课未提示 | 轮1 Q1/Q2 |
| G | 学分/学期数值臆造（数分B1 "4.0学分/大二上"） | 轮1 Q7 |
| H | 能力缺口：课表冲突检测/退补选压力评估无工具 | 轮1 Q3/Q6（待用户决定是否开发） |

## 修改与复测对照（按"一次一组共性问题"）

**组 A（选修组学期排序 + 学期中文标注）**
- 修改：`tools/advisor_tools.py::_recommend_grouped` 选修组 SQL 增加 `pc.term`，排序键改为 `(_term_urgency(term, current_yi), -rating_avg, -rating_count)`；`agents/qa/nodes.py` 新增 `_term_zh`（2秋→大二上），推荐 dump 的 hint 加中文学期；COMPOSE 规则"方案学期如实转述不得臆造"；`verify_tools.py` 选修组断言改为"紧迫度非降+同档评分降序"（32/32）。
- 复测（轮2）：Q1 选修组按 2春→…排序、标注"大二下学期（2春）"正确；Q2 同。✅ 解决。

**组 B（选课数据字段 + 摘要分支顺序）**
- 修改：`tools/course_tools.py::query_course_selection` fallback 补 time/location/semester；`nodes.py::_build_tool_summary` 选课分支输出时间/地点；COMPOSE 规则"有冲突必须指出"；**修复 Phase 2c 遗留 bug**：`message` 分支原排在明细分支之前吞掉选课/成绩明细，移到最后兜底。
- 复测（轮4）：Q3 列出 14 门课时间并识别 1 处疑似冲突+1 门时间不全；Q6 完整学分表（31.5 超 30 上限）+退课优先级。✅ 解决。

**组 C（来源标识）+ 组 D（先修课诚实性）**
- 修改：`tools/program_tools.py` 三工具返回加 `source="local"`；`nodes.py::_src` 增 local 标签，三个方案工具摘要加来源；COMPOSE 规则"先修/满足性无数据必须明说，严禁推断已修"。
- 复测（轮5）：Q4 原文"（数据来源：本地培养方案数据，非教务实时，最终以综合教务系统为准）"；Q5 原文"工具没有返回先修课要求，也没有你的成绩数据，暂时无法直接判断"，零编造。✅ 解决。

**组 E/F/G（展示质量收尾）**
- E：`_top_reviews` 已有 <10 字过滤；长文本语义无关评论无法低成本自动过滤——轮6 Q2 中 LLM 已自行标注"虽然只有一条评论且与课程无关"。记录为已知限制。
- F：COMPOSE 规则"必修组不得重排、不得称备选/可退"。
- G：核查 DB：数学分析B1 全部 31 页均为 6.0 学分（"4.0"是 LLM 臆造）；COMPOSE 规则"数值必须取自工具返回，不得臆造"。
- 复测（轮6）：Q2 必修组标注"培养方案要求，共6门，无法回避"、顺序与工具一致、无备选措辞；Q7 不再出现臆造学分。✅ 解决（E 为已知限制）。

## 过程中顺带修复的环境/健壮性问题

1. **Embedding 维度崩溃**（`knowledge/vector_store.py`）：API embedding（4096D）集合遇本地降级（768D）时查询抛异常——增加维度不匹配优雅返回（`mismatch: True` + 提示 rebuild_kb），add_documents 加维度冲突守卫。探针验证通过。
2. 轮1 首问超时根因：当时 LLM 平台网络超时（外部），网络恢复后复测正常。

## 最终验证（轮6 后）

- 回归：verify_tools 32/32 · test_fixes 30/30 · verify_nodes 29/29 · check_course_db 9/9 · 全量编译 ✅
- 六轮回答原文存档：`docs/选课测试_轮1~6_回答原文.md`

## 遗留（H，✅ 已开发：2026-08-15）

- **H 项已实现**：选课级精确冲突检测与退补选压力评估工具（`tools/selection_tools.py`：
  `check_course_conflict` / `evaluate_selection_pressure`，判定逻辑在 `utils/schedule_parse.py`）。
  节次级精确判定（周次/节次/时钟），周次不重叠不算冲突——轮4 中"力学B vs 热学B 疑似冲突"
  实测为该口径下的**非冲突**（11-12 周 vs 13-18 周）。LLM 链路实测（2026-08-15）：
  "我选的课有没有时间冲突？" → 意图「选课冲突」→ 14 门 0 冲突 + 时间不全如实说明；
  "我学分是不是超了，帮我看看要不要退课" → 意图「退补选评估」→ 31.0/30.0 超 1.0 + 退课建议。
  回归：verify_tools 39/39 · test_fixes 43/43 · verify_nodes 35/35。
- 遗留子项（记录为已知限制）：加课模拟仅对已有排课数据的课程有效（评课库/缓存无推荐课程
  排课时间，如实标注 adds_pending）；垃圾评论语义过滤需站点治理或引入 LLM 过滤（成本高，暂缓）。
