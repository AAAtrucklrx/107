# 会话交接摘要（压缩后的上下文锚点 · 2026-08-22 第三次刷新）

> **🔗 恢复锚点约定（2026-08-21 用户确认）：后续所有新会话/新对话一律从本文件开始无损恢复——先读本文件（事实基准），再读 `docs/接手日志.md` ⚡ 节（最新状态）。本文件为会话交接的唯一起点，不凭记忆臆断。**
> 本文件 = 对话全部历史的压缩事实基准。**此后所有工作一律以本文件为准，不凭记忆臆断。**
> 详细学习基线 `docs/学习报告.md`；滚动日志 `docs/接手日志.md`；总体开发方案 `docs/总纲与工具接口方案.md`。

## ① 项目与当前状态

- 小蜗 = 科大校园智能助手（107 杯），Streamlit + LangGraph 统一 QA（embedding_parse→think ≤4 轮→act→compose，THINK 规则 1-22）+ SQLite 双库 + ChromaDB 混合检索 + **29 个内置工具 + 生态工具（eco: 前缀）**。
- git HEAD：`a67d725`（2026-08-22 全模块实测加固），已推送 GitHub `https://github.com/AAAtrucklrx/107`。
- 2026-08-22 一天完成：P3-2 LLM 降级路由 / P3-3 学期常量配置化 / P3-4 重爬 SOP / P4-1 生态协议 / P4-2 render_link+校园导航页 / **青春科大爬取链路（7 接口破解）/ 活动个性化推荐 A→D 全链路（四因子+均衡补短板）** / 全模块 19 问实测（18 直接过+修 1 真 bug）。
- 主应用 LLM：`deepseek-v4-flash`；YOUNG_TOKEN（青春科大 JWT 137 位）**约 2026-08-29 到期**，到期前换新（F12→localStorage `pro__Access-Token-zsxc-base`）。

## ② 提交序列（2026-08-22 当日 10 个，此前见接手日志 ⚡ 节）

`3dfe303` P3-3 学期常量 → `f56903a` 日课表链路修复 → `74d5493` P3-2 LLM 降级路由 → `46d66ff` P3-4 重爬 SOP → `4345739` P4-1 生态协议 v1 → `3495154` P4-2 render_link+导航页 → `24a9f23` 青春科大爬取链路 → `6e3c938` 活动推荐 A→D 全链路 → `5816a56` 均衡感知推荐 → `a67d725` 全模块实测加固。

## ③ 数据现状

- `data/course_data.db`：5667 课程页（不合并同课多师）/ 44418 评论 / 2982 教师 / 632 培养方案；check 9/9。
- 知识库：80 篇 md → 769 分块；测试账号 PB25111691（26 成绩/14 选课）。
- **青春科大数据**（2026-08-22 打通）：`scripts/data/young_personal/young_snapshot.json`（报名中 19 + 已结束 1000 + 个人档案 + 兴趣标签 17 + 待参与/收藏/关注）；实时接口需有效 token。
- **用户模块学时**（推荐均衡信号）：德16/智17.5/体24/美17.5/劳40，总 115（scMyInfo period* 字段）。

## ④ 防幻觉要点（历史 17 条 + 2026-08-22 新增 7 条）

1-17. 见接手日志历史（smart_toy 真相 / git 提交约定 / 沙箱 tempfile / 硬编码画像已除 / 会话隔离 / legacy 已删 / source 中文标识 / 等级制成绩 / young 分页 / 性能 / icourse 不合并 / 评论缺口 / 断点续爬 / Windows 约定 / 并发环境 / CDP Edge / 测试版数据路径）。**新增：**

18. **注册表 29 内置 + 生态**：`agents/tool_registry.py` 合并 `tools/ecosystem/`（Spec 驱动，`eco:` 前缀，坏 Spec 拒载不炸）；旧记 24/26 已过时，以代码为准。
19. **⚠️ 平台越权接口禁用**：young `/personal/scExperience/list` 未按用户隔离（全库 35846 条他人数据）——**不调用不存储**；PoC 脚本 `scripts/tmp_young_leak_poc.py` 仅供用户向校团委反馈。
20. **YOUNG_TOKEN 运维**：JWT ~7 天有效；失效时 `query_activities` 自动回退本地快照（source 标"本地缓存"）；换 token = F12 → localStorage `pro__Access-Token-zsxc-base` 的 value（eyJ 开头 137 位）→ .env；`scripts/crawl_young.py` 重跑快照（个人接口偶发限流会存空段，重跑即恢复）。
21. **young 模块映射**（item_module 字典实测）：d德 z智 t体 m美 l劳；五维学时= scMyInfo 的 periodVirtue/Wisdom/Body/Pretty/Work；待参与接口 tobeInvolvedList **type 参数必传**。
22. **add_event 时间兜底**：`_enrich_add_event_args` 按 ISO 校验（非仅判缺失），非法即从原问题重解析+精确时钟提取（"下午3点到4点"→15:00~16:00，"晚上7点"→19:00~20:00）。
23. **测试断言教训**：any 断言勿用问句回显词（曾被"班级会议"回显误判 PASS）；qa_consistency banned 字段含 `.*` 时按正则匹配（"90分绩点"禁词=错误映射模式，完整对照表含 85~89→3.7 属正确）。
24. **运维教训（编辑接手日志）**：用 Edit 插入新条目时，new_string 必须以被替换的旧条目标题**完整结尾**，否则吞标题（本日发生 4 次均已修复）。

## ⑤ 验证基线（每次改动后全跑）

`check_course_db.py` 9/9 · `verify_tools.py` 39/39 · `test_fixes.py` 43/43 · `verify_nodes.py` 46/46 · 全量 py_compile ·
`verify_ecosystem.py` 10/10 · `verify_links.py` 12/12 · `verify_activities.py` 8/8（需有效 YOUNG_TOKEN，失效自动 SKIP 实拉项）· `verify_profile.py` 11/11 ·
需 LLM：`qa_consistency.py` 12/12 · `qa_new_docs.py` 10/10。
启动：`& $py -m streamlit run app_test.py --server.port 8502 --server.headless true`（后台 job）；重启前杀净 :8502 全部 PID（孤儿进程共享监听致随机路由旧实例）。

## ⑥ 关键文件地图

- QA 链：`agents/qa/graph.py`（run_qa）、`nodes.py`（think/act/compose、THINK 规则 1-22、_build_tool_summary、_enrich_*）、`intents.py`（15 类意图含"活动推荐"）
- 工具：`tools/course_tools.py`（query_daily_schedule 已重写）/ `advisor_tools.py` / `program_tools.py` / `schedule_tools.py`（import_schedule 已重写）/ `selection_tools.py` / `link_tools.py`（render_link）/ `activity_tools.py`（query_activities+快照回退）/ `ecosystem/`（协议 v1）
- 服务：`services/young_client.py`（**12 方法**：报名中/已结束/档案/标签/收藏/关注/待参与）、`activity_recommender.py`（四因子+MMR+FreeTimeMatcher 已修）、`activity_profile.py`（冷启动/行为权重/均衡）、`cas_client.py`、`session_ctx.py`、`llm_client.py`（熔断窗）
- 数据：`scripts/crawl_young.py`（个人快照）、`refresh_course_db.py`（评课重爬 SOP）、`rebuild_testdata.py`；`config/links.yaml`（19 官方链接）
- UI：`app.py`/`app_test.py`（模块分支：培养方案/校园导航）、`ui/chat.py`、`ui/links_page.py`（导航页）、`ui/activity_dialog.py`（四因子弹窗）

## ⑦ 待办（当前队列）

1. **P3-1 CAS 白名单**（外部申请，代码侧就绪）。
2. **YOUNG_TOKEN ~8/29 到期前换新**；正式版实现 `YoungAutoLoginProvider`（CAS 凭据自动换 JWT，顺带解锁全量个人接口）。
3. **个人已报名历史（itemized）**：待用户真实浏览器打开"学年成绩→个人成绩单"时抓包（headless 渲染不出）。
4. **向校团委反馈** scExperience/list 越权漏洞（用户已知悉，PoC 就绪）。
5. P5（卡片/晨报/方案页联动）与 P6（测试转正 dev_pipeline、文档定稿）待启动；生态试点 P4-4/5 已按用户指示取消。
