# 会话交接摘要（压缩后的上下文锚点）

> 2026-08-14 · 本文件是"小蜗"项目完全接手工作的压缩上下文：此后工作以本文件为唯一锚点，
> 不再依赖对话历史。详细学习基线见 `docs/学习报告.md`，滚动日志见 `docs/接手日志.md`。

## ① 项目与目标

小蜗 = 科大校园智能助手（107 杯比赛项目），Streamlit + LangGraph 统一 QA（embedding_parse→think→act→compose，≤4 轮）+ SQLite 双库 + ChromaDB 混合检索。
接手目标（goal-09ba9f74-753c-4d4a-abcd-c6458858de03）：Phase 0 基线 → Phase 1 功能 → Phase 2 架构债，按里程碑批量提交。**当前目标 blocked（用户要求暂停开发），用户说"继续"即恢复。**

## ② 授权与节奏约定

- 完全接手、独家驱动；指挥中心（Qoder/Claude Code/Codex）已暂停，但**工作区曾有并发进程**（曾把在途改动抢先提交为 19a8c6c）→ 动手前必查 `git status`。
- 垃圾清理：归档不直删（scripts/archive/、docs/archive/）。
- 提交流程：列清单+验证结果 → 用户在聊天中批准 → **pwsh 执行 git commit**（系统 git_commit 工具被沙箱阻断：无法建 .git/index.lock，勿再试）。身份：`-c user.name="xiaowo-dev" -c user.email="xiaowo@local"`。
- 会话管理约定：上下文接近 300K 时先刷新 `docs/接手日志.md` 再继续。

## ③ 已完成里程碑与验证基线

| 提交 | 内容 |
|---|---|
| `c997e82` | Phase 0：langgraph 依赖、.gitattributes、垃圾归档、crawl 数据移出 git、测试沉淀（XIAOWO_TEST_TMP）、硬编码画像修复、学习报告 |
| `6e7db1c` | Phase 1a：GPA 自动画像（≥3.7 硬核/≤2.7 冲分，显式优先）+ 已修课程兴趣推断 + 画像说明透出 |
| `8a02279` | Phase 1b：培养方案页课程一键点评（pending_query+module_switch）+ e2e 回归 + smart_toy 真相 |
| `aaa931d` | Phase 2a：多用户会话隔离——ContextVar 按学号分桶（CAS 客户端/选课画像），登录新建独立客户端，catalog 改无会话共享；calc_gpa 摘要补 source；.env.example 默认模型 deepseek-v4-flash（外部改动，用户批准纳入） |
| `53c50a7` | Phase 2b：legacy agents 9 文件退役（注册表迁至 agents/tool_registry.py）+ _resolve_program 双实现收敛至 tools/_program_resolve.py + 学习报告工具数修正为 24 |

验证基线（每轮全跑）：全量 py_compile ✅ · test_fixes 30/30 · verify_tools 31/31 · verify_nodes 11/11 · check_course_db 9/9；另做过真实链路实测：e2e_program_link（8502 实例）、GPA 个人链路（PB25111691 桶，GPA 3.27/60.5 学分）、注册表工具链（analyze_teacher）。

> 2026-08-14 追加：主应用 LLM 全面切换为 `deepseek-v4-flash`（.env/config 默认/README/学习报告已对齐；v4-flash 下 think 决策 JSON 与 compose 长回答实测通过）。`.env` 不入库，GitHub 上生效的是 config 默认值 + 文档；本地实际模型由 .env 决定。

## ④ 当前状态与恢复方式

- HEAD：`53c50a7`；工作区干净（仅 .dsh/ 会话目录、docs/e2e_v5_link_*.png 两张未跟踪截图、接手日志暂停声明未暂存——均有意的）。
- 目标 blocked；用户直接说"继续"→ 先 update_goal resume → 从 §⑤ 接续。

## ⑤ 未完成清单（优先级顺序）

1. **Phase 2c**：LLM 输出后处理加固（结构化输出探索）。
2. **P1-2**：app_test 测试版 monkey-patch（CASClient.get_my_program_tree、_fetch_real_* 进程级替换）治理。
3. **P2 清单**：推荐 N+1 查询批量化（_build_item 5 子查询×200 池）；_fetch_real_grades 等级制成绩（int(score) 丢"优秀/通过"）；young_client 分页未实现（只 pageNo=1）+ token 长度校验；source 标识统一（schedule/faq/advisor 无 source）；query_schedule 的 week 死参数；find_empty_room/query_exam 模拟数据误导提示；_norm_course_name 三处重复收敛；test_fixes 默认 mkdtemp 之外的 CI 兼容（已支持 XIAOWO_TEST_TMP）。
4. **Phase 1c**：培养方案数据刷新（crawl_jw_programs 需 jw 登录态，与用户确认时机）。
5. 收尾：学习报告 §7 风险表逐项闭环后更新；pip freeze 快照已存 docs/pip-freeze-2026-08-14.txt。

## ⑥ 决策记录（用户拍板，不可自行更改）

1. 学习报告：`docs/学习报告.md`，十节提纲。
2. 覆盖边界：源码与数据为主 + 垃圾产物归类。
3. 验证深度：静态分析 + 全套本地验证脚本（不含 LLM 端到端）。
4. 优先级：先稳基线（Phase 0）→ Phase 1 功能 → Phase 2 架构债。
5. 清理策略：归档不直删。
6. 提交节奏：里程碑批量，提交前聊天内批准。
7. 完全接手：指挥中心暂停，独家驱动（后改为"提交，但先停止开发"）。
8. .env.example 外部改动：批准纳入 Phase 2a 提交。
9. 上下文压缩：新建 `docs/会话交接摘要.md`（本文件）作为锚点，回复精简。

## ⑦ 坑位与教训

- **smart_toy 真相**：是 Streamlit 助手头像 Material 图标文字（用户头像为 "face"），非回答内容；e2e 断言必须用 `[data-testid="stChatMessageContent"]`。
- 沙箱（已解除，保留记录）：tempfile.mkdtemp 新建目录内 sqlite 无法建库 → XIAOWO_TEST_TMP 绕开；git_commit 工具无法建 index.lock → pwsh 提交。
- Windows：`py -3`/完整解释器路径（勿用 `python` 占位符）；PowerShell 无 `&&`；内联 `-c` 引号易炸 → 写脚本文件运行；Streamlit 改码需 taskkill 重启。
- 文档漂移点已修：9 表之谜（8 业务表+sqlite_sequence）、SEED_SQL 已清空、注册表 24 工具（非 22）、_do_login 死代码、db_manager 无 query_all、50 篇 md→220 分块。
- LLM 行为：未登录无偏好推荐问题 → clarify（符合 P1-3 修复预期，勿当 bug）；FAQ 命中候选时 think 直接 compose 不调 search_faq（勿当 bug）；calc_gpa 需 source 标注否则 LLM 把缓存数据当"教务处来源"陈述。
- 并发警惕：本会话分析期间文件曾被外部提交（19a8c6c）；改任何文件前先 git status。

## ⑧ 恢复步骤与关键命令

```bash
# 环境
$py = "C:\Users\Richelieu\AppData\Local\Programs\Python\Python314\python.exe"
# 测试临时目录（受限环境）
$env:XIAOWO_TEST_TMP = "F:\小蜗\scripts\data\tmp_test"

# 验证套件（无 LLM）
& $py scripts/check_course_db.py      # 9 项
& $py scripts/verify_tools.py         # 31 断言
& $py scripts/test_fixes.py           # 30 断言
& $py scripts/verify_nodes.py         # 11 断言

# 需 LLM：scripts/advisor_smoke.py / browser_e2e.py / qa_eval.py / e2e_program_link.py（--base）
# 启动：& $py -m streamlit run app_test.py --server.port 8502 --server.headless true
# 提交：git add ...; git -c user.name="xiaowo-dev" -c user.email="xiaowo@local" commit -m "..."
```

恢复第一步：git status 复核 → 从 §⑤ 第 1 项（Phase 2c）开始 → 完成后列清单经用户批准提交。
