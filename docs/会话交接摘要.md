# 会话交接摘要（压缩后的上下文锚点 · 2026-09-01 第五次刷新）

> **🔗 恢复锚点约定（2026-08-21 用户确认）：后续所有新会话/新对话一律从本文件开始无损恢复——先读本文件（事实基准），再读 `docs/接手日志.md` ⚡ 节（最新状态）。本文件为会话交接的唯一起点，不凭记忆臆断。**
> 本文件 = 对话全部历史的压缩事实基准。**此后所有工作一律以本文件为准，不凭记忆臆断。**
> 详细学习基线 `docs/学习报告.md`；滚动日志 `docs/接手日志.md`；总体开发方案 `docs/总纲与工具接口方案.md`；部署契约 `docs/部署规格与记录_2026-09-01.md`。

## ① 项目与当前状态

- 小蜗 = 科大校园智能助手（107 杯）：**React/Vite 四工作区 Web（FastAPI `/api/v1` + SSE，主应用）** + LangGraph 统一 QA（embedding_parse→think ≤4 轮→act→compose，THINK 规则 1-22）+ SQLite 双库 + ChromaDB 混合检索 + 28 内置工具 + 生态工具（`eco:` 前缀）+ 校园工具（campus/tools 申请审核流）。Streamlit（`app_test.py`）仅作回退入口。
- git HEAD：`980feb6`（feat: rebuild schedule and add campus tools admin，2026-09-01），main 与远端对齐；**工作区干净**（无未提交改动；未跟踪项均为数据/环境产物）。
- **部署完成（2026-09-01，Linux 服务器）**：Ubuntu 24.04 @ 114.214.241.119（内网 192.167.103.3），`competition + demo`，公网 `http://114.214.241.119:8850`（平台转发 8850→8000，origin 单值校验）；服务 = Web 8000 + worker + Streamlit 8502，管理 `deploy/server/{start_all,stop_all,status}.sh`（bwrap 沙箱无 systemd，nohup+pidfile）。
- 产品语义（用户定案，不得重新引入）：推荐只处理课程选择；课程范围硬条件，兴趣/工作量/教师/目标学期默认软排序且仅"只要/必须"硬过滤；复合"推荐且不冲突"只推荐；独立 `check_course_conflict` 保留；`force_calls`/`pending_force_calls` 已永久移除；身份只取当前用户 CAS/成绩档案，取不到不猜不继承；通用方案必须标注"专业通用参考"。
- 主应用 LLM：`deepseek-v4-flash`（科大平台）；YOUNG_TOKEN 已失效（活动链自动回退本地快照，刷新是独立运维事项）。

## ② 提交序列（2026-08-26 后最新；更早见接手日志 ⚡ 节）

`34e8858` P5-1 · `199449d` P5-2 · `a88f6a8` P5-3（已工作区回滚）· `f082aec` 精确课程全班级 · `0ab15ac` 同课多师分组 · `127f840` 课程关键词与同系列合并 · `77d367c` 流式打字机/可折叠思维卡/续写与编辑重生成+50 题金标测评 · `708805d` RAG 查询扩展+算分精排+检索事件+claim 引用核查 UI · **`980feb6` 课表重构+校园工具与管理后台**。

## ③ 数据现状（2026-09-01 服务器实测）

- `data/course_data.db`：5667 课程页 / 44418 评论 / 2982 教师 / 632 方案；check 9/9。
- 知识库：`knowledge/data` 1000 篇（办事140/就业35/教务405/生活72/升学87）→ 向量 1000 条；**注意**：向量库 768 维（text2vec 时代）与 API embedding 4096 维不匹配 → 按设计降级 BM25 检索（`knowledge/vector_store.py` 自动降级，检索命中正常）。
- 测试账号 PB25111691（demo，管理员）；`data/review.db` 新表 campus_tools / campus_tool_applications（demo 种子 3 行）/ campus_tool_audit。
- 青春科大快照：`scripts/data/young_personal/young_snapshot.json`（报名中 19 + 已结束 1000 + 档案 + 标签 17 + 模块学时德16/智17.5/体24/美17.5/劳40 总115）；实时接口需有效 token。
- 部署数据包：`scripts/data/xiaowo_deploy_data.zip`（42.9MB，随迁移历史保留）。

## ④ 防幻觉/运维要点（历次教训汇总）

1. 注册表 28 内置 + 生态：`agents/tool_registry.py` 合并 `tools/ecosystem/`（Spec 驱动，`eco:` 前缀，坏 Spec 拒载不炸）；旧记 24/26/29 内置均已过时，以代码为准。
2. **⚠️ 平台越权接口禁用**：young `/personal/scExperience/list` 未按用户隔离——不调用不存储；向校团委反馈是独立事项。
3. **YOUNG_TOKEN 运维**：JWT ~7 天有效；失效时活动链自动回退本地快照（source 标"本地缓存"）；换 token = F12 → localStorage `pro__Access-Token-zsxc-base` → .env → 重跑 `scripts/crawl_young.py`。
4. **young 模块映射**：d德 z智 t体 m美 l劳；五维学时 = scMyInfo periodVirtue/Wisdom/Body/Pretty/Work；待参与接口 tobeInvolvedList **type 参数必传**。
5. **add_event 时间兜底**：`_enrich_add_event_args` 按 ISO 校验（非法即重新解析+精确时钟提取）。
6. **测试断言教训**：any 断言勿用问句回显词；qa_consistency banned 含 `.*` 时按正则匹配。
7. **SSE 事件结构**：流式内容在 `answer.segment => data.data.markdown`，终态 `answer.completed => data.data.claims`（嵌套两层 data）。
8. **origin 单值校验**：`api/dependencies.py` hmac.compare_digest——访问地址变更必须同步 `.env` `XIAOWO_PUBLIC_ORIGIN` 并重启，否则写操作 403。
9. **Watt 证书轮换**：GitHub 通道 CA = `scripts/data/steamtools_ca_latest.pem`（2026-08-31 轮换，旧 pem 失效）；本机 git 为 GnuTLS 后端（无 `http.sslBackend=openssl`）。
10. **沙箱事实**：/etc、/root 只读（无 systemd、无系统 pip/ensurepip）、npm 缓存须指工作区 `.npm-cache`；venv 为 `.venv`（get-pip 引导）。
11. **测试断言 / 编辑教训**：any 勿用问句回显词；qa_consistency banned 含 `.*` 按正则匹配；Edit 插入条目时 new_string 必须以旧条目标题完整结尾。

## ⑤ 验证基线（每次改动后全跑；2026-09-01 Linux 实测）

- `pytest tests/web -q` **108/108**（认证/权限/SSE/审核/generation + 校园工具/学术新测试）
- `verify_tools.py` **44/44** · `test_fixes.py` **49/49** · `verify_nodes.py` **57/57**
- `check_course_db.py` 9/9 · `verify_ecosystem.py` 10/10 · `verify_links.py` 12/12 · `verify_profile.py` 11/11 · `verify_time_parser.py` 17/17 · `verify_security_ui.py` 20/20 · `verify_activities.py` 8/8（实时或快照降级）
- `verify_web_load.py` 通过（SSE=100 / 峰值 30 / 503 / 完成 100）；`init_check.py` 通过（含 db_manager 轻量迁移）
- 需 LLM（向外部发学号/画像，须授权）：`qa_consistency.py` 12/12 · `qa_new_docs.py` 10/10（最近基线，未重跑）
- 命令一律：`/root/Desktop/小蜗/.venv/bin/python scripts/<suite>.py`；部署冒烟 `deploy/server/status.sh` + `curl :8000/api/v1/health/{live,ready}`。

## ⑥ 关键文件地图

- QA 链：`agents/qa/graph.py`（run_qa）、`nodes.py`（think/act/compose、THINK 规则 1-22、_build_tool_summary、_enrich_*）、`intents.py`（15 类意图含"活动推荐"）
- 工具：`tools/course_tools.py`（课表已重构）/ `advisor_tools.py` / `program_tools.py` / `schedule_tools.py` / `selection_tools.py` / `link_tools.py`（render_link）/ `activity_tools.py`（query_activities+快照回退）/ `ecosystem/`（协议 v1）
- Web：`xiaowo_web/main.py`（lifespan：demo 审核种子 + 校园工具种子）、`api/routers/`（auth/chat/campus/academic/admin/review/system）、`campus/`（CampusService + CampusToolStore 申请审核流）、`academic/`、`review/`（审核/发布）、`worker/`（ingestion/refetch/publish 三 worker）
- 服务：`services/young_client.py`（12 方法：报名中/已结束/档案/标签/收藏/关注/待参与）、`activity_recommender.py`（四因子+MMR）、`activity_profile.py`（冷启动/行为权重/均衡）、`cas_client.py`（认证专业/年级规范化与实例级个人方案树）、`session_ctx.py`、`llm_client.py`（熔断窗）
- 数据/脚本：`scripts/crawl_young.py`（个人快照）、`refresh_course_db.py`（评课重爬 SOP）、`config/links.yaml`（19 官方链接）；`config.py`（SEMESTER=2026-2027-1，env `XIAOWO_SEMESTER_START` 覆盖）
- 部署：`deploy/server/{start_all,stop_all,status}.sh`（日志 `deploy/server/logs/`）、`docs/部署规格与记录_2026-09-01.md`

## ⑦ 待办（当前队列）

1. **P3-1 CAS 白名单**（网络中心外部申请，代码侧就绪；真实 CAS 的硬前置）。
2. **YOUNG_TOKEN**：失效，需用户 F12 换新后重跑 crawl_young（活动推荐已用本地快照兜底）。
3. **联网 sidecar 部署**（SearXNG/Crawl4AI + attestation 后才开联网；Docker 镜像拉取路径待测）。
4. **itemized（个人已报名历史）接口**：需用户真实浏览器"学年成绩→个人成绩单"时抓包（headless 渲染不出）。
5. **向校团委反馈** scExperience/list 越权漏洞（用户已悉；当前无本地 PoC 文件）。
6. **P6-1**：将完整验证集纳入 `scripts/dev_pipeline`。
7. **可选**：向量库用 qwen3-embedding 重建（数据资产变更，需用户拍板）；LLM 客户端 30s 超时放宽（用户已拍板暂不改）。
