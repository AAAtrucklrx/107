# 会话交接摘要（压缩后的上下文锚点 · 2026-08-14 第二次压缩）

> **🔗 恢复锚点约定（2026-08-21 用户确认）：后续所有新会话/新对话一律从本文件开始无损恢复——先读本文件（事实基准），再读 `docs/接手日志.md` ⚡ 节（最新状态）。本文件为会话交接的唯一起点，不凭记忆臆断。**
> 本文件 = 对话全部历史的压缩事实基准。**此后所有工作一律以本文件为准，不凭记忆臆断。**
> 详细学习基线 `docs/学习报告.md`；滚动日志 `docs/接手日志.md`。
> 更新（2026-08-15）：新维护会话已接手，最新状态（HEAD `3e60862`、待办队列、验证基线）以 `docs/接手日志.md` ⚡ 节为准。
> 更新（2026-08-21）：HEAD `33df671`；知识库 80 篇/769 块；本会话完成 4 个提交（知识库补录第二轮/测试数据路径/评课思维链/检索决策加固），详见接手日志 ⚡ 节。

## ① 项目与当前状态

- 小蜗 = 科大校园智能助手（107 杯），Streamlit + LangGraph 统一 QA（embedding_parse→think→act→compose，≤4 轮）+ SQLite 双库 + ChromaDB 混合检索。
- git HEAD：`33df671`，已推送 GitHub `https://github.com/AAAtrucklrx/107`（远端=本地）。工作区干净（仅 .dsh/、docs/ 截图有意未入库）。
- 主应用 LLM：`deepseek-v4-flash`（.env 与 config 默认值一致；早期 deepseek-chat 平台侧路由至 v4-flash-ascend）。
- 目标 goal-09ba9f74 已 complete（完全接手任务整体办结）。

## ② 提交序列（GitHub main，共 17 个）

`19a8c6c`(接手前) → `c997e82` Phase 0 → `6e7db1c` 1a 个性化 → `8a02279` 1b 联动 → `aaa931d` 2a 会话隔离 → `53c50a7` 2b legacy 退役 → `3581c2d` 切 v4-flash → `d9c8771` 压缩文档 → `5d4b744` 2c 输出加固 → `c988b9f` P1-2 治理+source 标签 → `096975c` N+1+norm 收敛 → `8a8c3c6` young 分页 → `f2250d2` week 废弃 → `1412c35` 等级制成绩 → `1162510` 删假数据 → `00664ca` Phase 1c 方案刷新 → `0faad83` 评课库不合并重建。

## ③ 数据现状（最新重建）

- `data/course_data.db`：**5667 个课程页**（每 icourse 页一行，**不合并同课多师**）、44418 评论、2982 教师、5429 course_teachers、632 培养方案、45168 方案-课程关联；9/9 完整性通过。
- 知识库：**80 篇 md → 769 分块**（2026-08-16 补录 8 篇权威资料 + 2026-08-21 教秘文档名单拆块重构；元数据零缺失）；测试账号 PB25111691：26 成绩/14 选课（SEED_SQL 已清空）。
- 培养方案：jw 全量刷新过（1201 抓取/0 失败，入库 2022 级后 632）。

## ④ 历次工作问题的结论（防幻觉要点）

1. **smart_toy 真相**：是 Streamlit 助手头像 Material 图标文字（用户头像 "face"），非回答内容；e2e 断言必须用 `[data-testid="stChatMessageContent"]`。
2. **git_commit 工具被沙箱阻断**（无法建 .git/index.lock）→ 一律 pwsh 提交，且**先列清单经用户在聊天中批准**；身份 `-c user.name="xiaowo-dev" -c user.email="xiaowo@local"`。
3. **沙箱 tempfile**：mkdtemp 新目录内 sqlite 建库失败 → `XIAOWO_TEST_TMP=F:\小蜗\scripts\data\tmp_test` 绕开（test_fixes 已内置支持）。
4. **硬编码画像已除**：`_extract_profile` 不再填"计算机科学/大二/人工智能"假值；未登录无偏好 → clarify/collect_preferences。
5. **多用户隔离**：`services/session_ctx.py` ContextVar 按学号分桶（CAS 客户端/advisor 画像）；run_qa 入口 set/reset。
6. **legacy agents 9 文件已删**；注册表在 `agents/tool_registry.py`（24 工具）；`_resolve_program` 唯一实现 `tools/_program_resolve.py`。
7. **来源标识**：工具摘要 source 已中文化（"本地缓存/模拟数据，仅供参考"）；空教室/考试的**假数据 fallback 已删除**，改如实"接口暂时不可用，暂无数据"。
8. **等级制成绩**：`student_grades.score_text` 列（老库轻量迁移）；等级制用 jw 自带 gp，缺 gp 跳过告警；展示用 score_display。
9. **young_client**：翻页取满（MAX_PAGES=20）、短 token ValueError、`_to_int` 数字容错。
10. **性能**：推荐按需构建（_build_item 只建目标条数）；`_norm_course_name` 唯一实现 `utils/course_name.py`（并集口径）。
11. **icourse 数据不合并**（`0faad83`）：courses 表去掉 UNIQUE(name,dept)，每页一行；`_pool_rows`/选修组按归一课名去重；`analyze_teacher` 课程模式**跨页聚合**教师/均分/评论。
12. **评论缺口结论**：每课约 10% 的"宣称评论数"缺口是 icourse 站点自身隐藏/删除（登录后同样看不到），爬虫已取 DOM 全部评论，无法再补——如实记录，勿再排查。
13. **断点续爬坑**：`crawl_icourse.py` 加 `--force`（列表/详情强制重抓）；重爬流程 = `list --force` → `detail --force --shard 1..4 --shards 4` → `build_course_db.py`。
14. **Windows 约定**：`py -3` 或完整解释器路径 `C:\Users\Richelieu\AppData\Local\Programs\Python\Python314\python.exe`；PowerShell 无 `&&` 用 `;`；内联 `-c` 引号易炸 → 写脚本文件运行；bat 中文乱码 → 纯英文。
15. **并发环境**：指挥中心（Qoder 等）已暂停，本会话独家驱动；动手前必查 `git status`。
16. **调试 Edge**：CDP 9223，profile `C:\Users\Richelieu\AppData\Local\Temp\xiaowo_cdp_profile`；一键脚本 `scripts/start_cdp_edge.bat`（纯英文）；当前窗口开着且已登录 jw+icourse（用后请用户关闭）。
17. **测试版**：`app_test.py` 伪登录 PB25111691（离线开关 `set_offline_mode` + `CASClient._injected_program_tree`），数据优先工作区固定备份 `scripts/data/xiaowo_personal/data.json`（2026-08-21 起，沙箱随机 TEMP 不可靠），回退 `%TEMP%\xiaowo_personal\data.json`。

## ⑤ 验证基线（每次改动后全跑）

`check_course_db.py` 9/9 · `verify_tools.py` 32/32 · `test_fixes.py` 30/30 · `verify_nodes.py` 46/46 · 全量 py_compile。
需 LLM 的实测：`qa_consistency.py` 12/12 · `qa_new_docs.py` 10/10 · `e2e_program_link.py --base http://localhost:8502`（联动）；`qa_eval.py`/`advisor_smoke.py`。
启动：`& $py -m streamlit run app_test.py --server.port 8502 --server.headless true`（后台 job）；重启前必须 `netstat -ano | Select-String ":8502.*LISTENING"` 杀光全部 PID（孤儿进程共享监听致浏览器随机路由旧实例）。

## ⑥ 关键文件地图

- 推荐算法：`tools/advisor_tools.py`（_pool_rows 去重、_recommend_flat/_recommend_grouped、recommend_courses、compare_courses、analyze_teacher 跨页聚合、_infer_preference GPA 画像）
- QA 链：`agents/qa/graph.py`（run_qa）、`nodes.py`（think/act/compose、_build_tool_summary、_strip_rule_prefix、_extract_profile）
- 课程/成绩：`tools/course_tools.py`；方案：`tools/program_tools.py`；日程：`tools/schedule_tools.py`
- 服务：`services/service_container.py`（CAS 分桶）、`session_ctx.py`、`cas_client.py`、`young_client.py`、`activity_recommender.py`
- 数据：`scripts/build_course_db.py`、`crawl_icourse.py`、`crawl_jw_programs.py`、`check_course_db.py`；`database/schema_course.sql`（不合并）、`schema.sql`
- UI：`app.py`/`app_test.py`、`ui/chat.py`、`ui/program_page.py`、`ui/activity_dialog.py`

## ⑦ 待办（当前队列）

1. **部署打通真实 CAS 登录**（需 CAS service 白名单；个人数据链路已由测试备份 + SQLite 降级验证）。
2. **定期巡检**：P2-4 LLM 平台单点依赖；schedule_tools 硬编码开学日期每学期巡检（P2-13）；评课快照定期重跑（crawl_icourse.py --force）。
3. **测试脚本沉淀**：selection_test_round.py / test_round_full.py 整理纳入 dev_pipeline TEST_SCRIPTS（qa_new_docs.py 已转正）。
