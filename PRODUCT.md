# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 首要用户是中国科学技术大学学生。他们在选课、查看学业进度、安排校园事务和核实校内信息时使用小蜗。
- 匿名校园用户可以使用不涉及个人数据的公共问答与校园服务能力。
- 已登录学生可以使用个人成绩、课表、培养方案、日程与服务端会话历史。
- 经授权的审核管理员负责公开网页证据的清洗、逐块审核、发布、失效与回滚。

## Product Purpose

小蜗是科大校园智能助手，也是面向学生日常任务的科大学术工作台。它把校园知识问答、个人学业数据、课程与培养方案工具、校园服务入口和有来源约束的联网检索放进一个连续工作流。成功意味着用户可以快速完成真实任务，并清楚知道答案来自哪里、是否可靠、是否为演示或降级数据。

## Positioning

小蜗的差异化机制是科大专用工具和个人学业数据与严格证据门槛的结合。系统只在本地资料不足、权威性不足或问题明确要求最新信息时联网；确定结论必须得到一个审核通过的官方一手来源，或两个独立且一致的可靠来源。证据不足或来源冲突时明确说明，不让模型自行猜测。

## Operating Context

- 四个稳定工作区为 `/` 问小蜗、`/academic` 我的学业、`/campus` 校园服务和 `/review` 知识审核。
- 主界面是 React + Vite + TypeScript SPA，由 FastAPI `/api/v1` 同源提供 API、认证和 SSE 流式事件。
- 学生会在桌面和手机上高频查看聊天、成绩、课表、培养方案与办事入口；审核人员主要处理高密度证据、差异与分块状态。
- 比赛环境使用持续标识的合成演示身份 `PB25111691`。正式身份接口保留 CAS Provider，但在可信 HTTPS 域名和白名单具备前不得启用。
- 匿名历史只保存在当前浏览器且不会在登录后自动合并。已登录历史在服务端保存 90 天并可由用户删除。

## Capabilities and Constraints

- 支持校园问答、成绩与 GPA、课表、课程推荐、独立课表冲突检查、培养方案、日程、活动推荐和已核实官方入口。
- 课程范围是推荐硬条件。兴趣、工作量、教师和目标学期默认只参与软排序，只有用户明确使用“只要”或“必须”时才成为硬过滤。复合“推荐且不冲突”只进行推荐并说明未检查课表；独立冲突检查继续保留。
- 登录后的专业和年级只能来自当前用户的 CAS 或成绩档案。个人培养方案不可用时只允许展示已验证身份对应的专业通用参考，并明确标注它不是个人培养方案。
- 公开证据队列不保存原始用户问题、账号标识或私密上下文。审核发布采用逐块 `pending / approved / rejected` 状态、TTL、generation、回滚和不可变原始快照。
- 只抓取公开 HTTP/HTTPS 页面，遵守 robots 与速率限制，阻断内网、回环、云元数据及重定向后的私网目标。抓取器不得获得 CAS Cookie 或任何校内凭证。
- 开发机尚未运行 SearXNG 与 Crawl4AI sidecar，界面不得伪装联网能力已经可用。
- 首期支持公开 HTML 与文本型 PDF；不支持上传、OCR、图片理解、视频或音频解析，也不展示思维链。
- 不造假是硬约束。接口不可用、数据缺失、缓存降级和证据不足必须如实显示来源与限制。

## Brand Commitments

- 产品名称与定位是“小蜗 · 科大学术工作台”。保留现有定制“小蜗”中文字标及克制的几何旋纹，不使用官方校徽，也不引入卡通吉祥物。
- 界面是安静、精确、任务导向的科大学术工作台。首屏直接进入应用，不做营销首页，不套用通用后台模板。
- 默认浅色并提供完整深色主题。主操作色使用科大蓝 `#034EA1`，联网与检索使用青色 `#099BA8`，其余采用中性灰和独立状态色。
- 蓝色只表达主操作，青色只表达联网或检索语义。真实性状态必须同时通过文字、图标和结构表达，不能只依赖颜色。
- 动效只服务状态变化、层级反馈和工作区过渡，并尊重减少动态效果偏好。
- 可改进界面文案的清晰度，但不得改写事实、能力边界、法律或安全含义。

## Evidence on Hand

- `frontend/src/assets/xiaowo-wordmark.svg` 是现有品牌字标。
- `frontend/src/index.css`、`frontend/src/components/AppShell.tsx` 与四个 workspace 是现有视觉和交互事实。
- `docs/小蜗_Web应用与联网RAG技术规格.md` 是 Web、认证、联网证据与审核契约。
- `docs/tool-specs.md` 与 `docs/总纲与工具接口方案.md` 记录工具行为与产品边界。
- 仓库包含 80 篇权威知识文档、审核来源白名单、真实功能测试以及桌面和移动端 E2E 脚本。
- 当前没有正式品牌手册、真实校园摄影、用户评价、公开使用分析或可公开成效指标。未来界面不得虚构这些证据。

## Product Principles

1. 先完成任务，再表达品牌；品牌存在于精确细节，不妨碍扫描和操作。
2. 真实性优先于流畅感；来源、身份、演示、缓存、冲突和证据不足始终可见。
3. 个人数据严格隔离；匿名、演示和真实身份之间不猜测、不继承、不自动合并。
4. 复杂能力渐进披露；默认路径简洁，来源、限制、审核和高级控制按需展开。
5. 桌面与移动端共享同一功能真相，但根据设备重新组织密度和操作顺序。

## Accessibility & Inclusion

- 目标采用 WCAG 2.2 AA。所有核心流程必须支持键盘操作、可见焦点、语义化标签、ARIA live region 和非颜色状态表达。
- 使用自托管 CJK 字体，保证中文长文本、数字、英文缩写和最长按钮标签在 320px 以上视口中不重叠或裁切。
- 所有非必要动画尊重 `prefers-reduced-motion`；浅色和深色主题均保持文本、控件、焦点与状态对比度。
- 高密度课表、学业与审核内容在小屏上需要明确的重排或可理解的横向滚动提示，不能简单压缩桌面表格。

## Open Decisions

- 当前没有正式用户侧量化成功指标。比赛验收之外的留存、任务完成率和答案满意度需在取得真实使用数据后定义。
