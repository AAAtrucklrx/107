# 密钥与敏感配置（不入库）
.env
deploy/sidecars/.env

# 运行态数据库
database/*.db
database/*.db-shm
database/*.db-wal
data/*.db
data/*.db-shm
data/*.db-wal
data/young_dialog_log.json
data/morning_brief_log.json
data/web_evidence/

# 爬虫原始数据（可重建）
scripts/data/

# jw 登录态浏览器 profile（含会话 cookie, 绝不可入库）
scripts/.jw_profile/
scripts/.jw_profile2/

# 向量库持久化目录
knowledge/chroma_db/

# Python 缓存
__pycache__/
*.pyc

# 本地审查/工具产物
.qoder/

# 知识库备份目录
knowledge_backup_*/

# 本地 IDE/工具配置
.claude/

# 调试脚本与截图归档（Phase 0 起，垃圾产物不入库）
scripts/archive/
docs/archive/

# 调试日志
*.log

# 子代理抓取临时产物（可重建，不入库）
_ustc_scrape/

# 新 Web 前端与测试产物
frontend/node_modules/
frontend/dist/
frontend/playwright-report/
frontend/test-results/
frontend/*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts
.pytest_cache/

# 临时/一次性脚本（2026-09-17：曾被 git add -A 误提交 105 个，撤回并忽略）
scripts/tmp_*.py

# 截图与临时产物（2026-09-17：`git add -A docs` 误把 61 个截图提交，事后撤出并补规则。
# 与 2026-09-17 早先 `scripts/tmp_*.py` 那次同类教训 —— 提交前用 `git status` 逐条看，
# 不要对目录用 `git add -A`。）
docs/qa/
docs/tmp_*
docs/e2e_v5_link_*.png
docs/选课测试_轮*_Q*.png
# 环境与数据产物（2026-09-17 补）。这些路径此前是"未跟踪但不被忽略"的状态 ——
# 对目录用 `git add -A` 会把它们一起提交（2026-09-17 两次同类事故：scripts/tmp_*.py、
# docs 截图产物）。规则：**提交前只看 `git status`，不要对目录用 -A。**
.venv/
.dsh/
.zcode/
.tools/
.models/
.npm-cache/
.pytest_tmp/
.impeccable/
data/
database/*.db-wal
database/*.db-shm
scripts/qa60_run.py
scripts/data/

# 服务器运维脚本的运行态产物（2026-09-22：deploy/server/ 一直未入库，日志/pid/状态不入库）
deploy/server/logs/
deploy/server/run/
deploy/server/*.out
deploy/server/*.log
deploy/server/__pycache__/
