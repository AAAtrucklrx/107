# 小蜗项目交接报告

> 更新日期：2026-08-22 · 当前版本：v2.2（QA LangGraph v3.0 + 工具生态 + 活动个性化推荐）
> 比赛项目：107 杯 · 面向中国科学技术大学师生的校园智能助手
> 会话恢复：从 [docs/会话交接摘要.md](docs/会话交接摘要.md) 开始（唯一起点），最新状态见 [docs/接手日志.md](docs/接手日志.md) ⚡ 节
> 总体开发方案：[docs/总纲与工具接口方案.md](docs/总纲与工具接口方案.md)（P3-2/3/4、P4-1/2/3 已完成）

---

## 1. 项目概述

小蜗是基于科大 LLM 平台（`api.llm.ustc.edu.cn`）构建的校园智能助手。核心链路为**统一 QA LangGraph**：意图分类 + 混合召回双通道 → LLM 自主决策循环（≤4 轮，THINK 规则 1-22 + 确定性路由兜底）→ 工具执行 → 统一合成回答。所有降级/缓存数据均带来源标识（实时数据 / 本地缓存 / 第三方工具）。

**v2.2 新增能力线（2026-08-22）**：LLM 平台降级路由（断网不瘫）、生态工具注册协议（学生自制工具可投稿接入）、官方入口跳转工具 + 校园导航页、**青春科大（第二课堂）实时活动数据接入与个性化推荐**（平台兴趣标签 + 行为学习 + 模块学时均衡补短板 + 课表空闲匹配四因子）。

## 2. 技术架构

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Streamlit | 侧边栏模块切换（含培养方案页、**校园导航页**）+ 对话 + 四因子活动弹窗 |
| 智能体 | LangGraph | `embedding_parse → think → act → compose`；THINK 规则 1-22；确定性路由（选课冲突/退补选/教师开课/日课表）兜底 |
| 工具 | **28 内置 + 生态** | `agents/tool_registry.py` 合并 `tools/ecosystem/`（Spec 驱动 `eco:` 前缀，投稿制） |
| 知识库 | ChromaDB 混合检索 | 80 篇权威文档 → 769 分块；**维度不匹配自动降级 BM25-only**（断网 RAG 可用） |
| 数据 | SQLite 双库 | `database/xiaowo.db`（主库/日程/**活动偏好两表**）+ `data/course_data.db`（评课库） |
| 外部服务 | CAS、jw、**young 青春科大**、科大 LLM | young 协议已逆向（AES-128-CBC），12 个数据方法 |
| 可靠性 | **双层熔断** | llm_client 进程级 600s 熔断窗（仅连接级失败）+ 轮内 llm_down；connect 5s 快败 |

关键入口：`app.py`（正式版）/ `app_test.py`（测试版，伪登录 PB25111691 + 离线数据注入）/ `agents/qa/graph.py`（run_qa）/ `agents/qa/nodes.py`（四节点与全部规则）。

## 3. 数据源说明

| 数据源 | 内容 | 现状（2026-08-22） |
|---|---|---|
| 评课库 `data/course_data.db` | icourse.club + jw 培养方案 | 5667 课程页（不合并同课多师）/ 44418 评论 / 2982 教师 / 632 方案；一键重爬 SOP `scripts/refresh_course_db.py` |
| 个人库 `database/xiaowo.db` | jw 个人数据缓存 + 日程 + **活动偏好** | 测试账号 26 成绩 / 14 选课；activity_preferences / activity_interactions 两表 |
| **青春科大 young.ustc.edu.cn** | **第二课堂数据** | 协议客户端 12 方法：报名中/已结束活动、**个人档案（德智体美劳五维成绩+学时+社团）**、兴趣标签 17、收藏/关注/待参与；快照 `scripts/data/young_personal/young_snapshot.json`（`crawl_young.py` 维护）；token 约 7 天有效期，失效自动回退快照 |
| 知识库 `knowledge/data/` | 80 篇权威文档 | 769 分块，来源可追溯，官方 URL 透出 |
| 官方链接清单 `config/links.yaml` | 19 条已核实官方入口 | render_link 与导航页共用，禁止生成清单外 URL |

## 4. 功能清单

| 模块 | 功能 | 状态 |
|---|---|---|
| 📚 智能问答 | 80 篇 RAG，回答附官方来源网址；断网 BM25 降级 | ✅ |
| 📊 课业助手 | 成绩/GPA/课表/**某天课表（精确时钟）**/考试/空教室/课程检索 | ✅ |
| 🔍 选课顾问 | 培养方案分组推荐、同课多师对比、教师分析、冲突检测、退补选压力评估 | ✅ |
| 🎓 培养方案 | 总览/学期规划/培养进度三合一页 | ✅ |
| 📅 日程管理 | 增删查/冲突检测/课表导入（**13 节次精确时钟+开学日期配置化**）/自然语言时间解析（"明天下午3点到4点"精确落点） | ✅ |
| 🎪 **活动推荐** | **青春科大实时数据**：对话查询（query_activities）+ 每日弹窗四因子个性化推荐（紧迫度/课表空闲/个性化/热度）+ **模块学时均衡补短板**（德智体美劳）+ 行为学习（shown/asked 流水）+ 快照回退 | ✅ 2026-08-22 |
| 🔗 **官方入口** | render_link 跳转工具（强操作类诉求给官方入口+小蜗辅助）+ 校园导航页（19 条已核实链接） | ✅ 2026-08-22 |
| 🧩 **生态工具** | 注册协议 v1（Spec+函数三步接入，`eco:` 前缀强制署名）；echo 自检用例；投稿 README | ✅ 2026-08-22 |
| 🛡 可靠性 | LLM 平台断网/慢三层降级（不假死、摘要直出、熔断不传染） | ✅ 2026-08-22 |
| 🧪 测试版 | `app_test.py` 伪登录 + 离线数据注入 | ✅ |

## 5. 2026-08-22 开发记录（10 提交）

| 提交 | 内容 |
|---|---|
| `3dfe303` | P3-3 学期常量配置化（SEMESTER 入 config；import_schedule 复用官方 13 节次表含晚课；修 StructuredTool 直调与 is_recurring 星期语义两存量 bug） |
| `f56903a` | 日课表链路修复（query_daily_schedule 降级解析重写；摘要显换算时钟；"周X+安排"确定性路由防漂移） |
| `74d5493` | P3-2 LLM 降级路由（快败+双层熔断+连接级判定；断网 RAG 降级 BM25-only；意图关键词兜底；断网实测 73.7s/32.5s→34s/0.1s） |
| `46d66ff` | P3-4 评课重爬 SOP（refresh_course_db.py：preflight→list→detail 4 分片→build→check，断点续跑） |
| `4345739` | P4-1 生态工具注册协议 v1（加载器/echo/投稿 README/THINK 规则 20/摘要署名） |
| `3495154` | P4-2 render_link + 校园导航页（19 官方链接；THINK 规则 21 禁止编造 URL；ui/links_page.py） |
| `24a9f23` | 青春科大爬取链路（7 接口攻克；young_client 12 方法；crawl_young 快照 716KB；**发现平台越权接口已停用并报备**） |
| `6e3c938` | 活动个性化推荐 A→D（FreeTimeMatcher 修复；query_activities 工具+规则 22；偏好画像两表+四因子推荐；快照回退） |
| `5816a56` | 均衡感知推荐（模块学时 d德z智t体m美l劳；缺口归一化补短板，最高模块不加持） |
| `a67d725` | 全模块 19 问实测（18 直接过；修 add_event 非 ISO 时间 bug；qa_consistency 断言正则化） |

## 6. 开发经验与坑位

历史经验（实测闭环流程、Windows 环境要点、常见坑位、指挥中心分工）见本节 v2.1 存档与 `docs/接手日志.md`。**2026-08-22 新增**：

| 坑位 | 修复模式 |
|---|---|
| LLM 断网假死（重试×超时叠加分钟级） | max_retries=1 + connect 5s + 进程熔断窗（仅连接级失败开窗，读超时不传染） |
| 断网时 RAG 全瘫（本地模型与库维度不匹配返回空） | 维度不匹配降级 BM25-only；嵌入器模块级缓存 |
| 旧时间格式正则（`:(\d)(...)`）与现行 jw 格式永不相配 | 统一走 `utils/schedule_parse`（query_daily_schedule 与 FreeTimeMatcher 两处同族 bug） |
| think 漏传/原样传参数（生态工具缺参、add_event 非 ISO） | _TOOL_LIST 带参数提示；enrich 层 ISO 校验+重解析+时钟提取 |
| 测试断言误判（问句回显词撞 any；正确对照表撞禁词） | any 勿用回显词；banned 支持 `.*` 正则模式 |
| 编辑接手日志吞上一条目标题 | Edit 的 new_string 必须以旧条目标题完整结尾 |

## 7. 已知问题与限制

- 真实 CAS 登录部署需 service 白名单（P3-1，外部申请事项）；比赛可走 `app_test.py` 离线路径
- YOUNG_TOKEN 约 7 天有效期，需定期换新（失效自动回退快照不瘫）；正式版规划 CAS 自动换 JWT
- 个人"已报名历史"明细接口未攻克（成绩单页 headless 渲染不出，待真实浏览器抓包一次）
- **平台安全**：young `scExperience/list` 存在水平越权（全库可读），小蜗已停用该接口、不存储他人数据；用户将向校团委反馈
- 评课数据为快照（重爬 SOP 已脚本化，选课季前执行）；教师均分小样本偏差提示口径保留

## 8. 后续路线（见总纲方案，P5/P6 待启动）

1. P5：工具结果卡片化、晨报（课表+DDL+活动）、培养方案页×选课顾问联动
2. P6：测试脚本转正入 dev_pipeline、比赛演示基线、文档定稿
3. 正式版：`YoungAutoLoginProvider`（CAS 自动换 JWT）、CAS 白名单部署

## 9. 验证基线（2026-08-22）

`check_course_db` 9/9 · `verify_tools` 39/39 · `test_fixes` 43/43 · `verify_nodes` 46/46 · `verify_ecosystem` 10/10 · `verify_links` 12/12 · `verify_activities` 8/8 · `verify_profile` 11/11 · `qa_consistency` 12/12 · `qa_new_docs` 10/10 · 全量 py_compile；UI 实测：全模块 19 问（2026-08-22）、断网冒烟、活动弹窗个性化理由。

---

> 快速开始见 [README.md](README.md)；Tool 规格见 [docs/tool-specs.md](docs/tool-specs.md)；开发日志见 [docs/接手日志.md](docs/接手日志.md)。
