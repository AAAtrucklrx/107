# 小蜗项目交接报告

> 更新日期：2026-08-14 · 当前版本：v2.1（QA LangGraph v3.0）
> 比赛项目：107 杯 · 面向中国科学技术大学师生的校园智能助手

---

## 1. 项目概述

小蜗是基于科大 LLM 平台（`api.llm.ustc.edu.cn`）构建的校园智能助手，覆盖四大模块：智能问答、课业助手、选课顾问、日程管理，另含培养方案可视化页面与校团委活动推荐。

核心链路为**统一 QA LangGraph**：意图分类 + 混合召回双通道 → LLM 自主决策循环（≤4 轮）→ 工具执行 → 统一合成回答。所有降级/模拟数据均带来源标识（实时数据 / 本地缓存 / 模拟数据）。

## 2. 技术架构

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Streamlit | 侧边栏模块切换 + 对话式交互 + 培养方案页 |
| 智能体 | LangGraph | `embedding_parse → think → act → compose`；think 由 LLM 决策 clarify/retrieve/call_tool/compose，失败降级确定性规则 |
| 知识库 | ChromaDB + SentenceTransformer/qwen3-embedding | 50 篇校园知识库文档、220 条向量分块 |
| 数据 | SQLite 双库 | `database/xiaowo.db`（主库：个人数据/日程/知识库）+ `data/course_data.db`（评课库 9 表） |
| 外部服务 | CAS 统一认证、jw 教务 API、young 校团委平台 | CAS 登录后拉取真实成绩/课表/方案树 |

关键入口文件：`app.py`（正式版）、`app_test.py`（测试版：模拟登录 + 注入个人备份数据，绕过 CAS）、`agents/qa/graph.py`（QA 图装配与 `run_qa` 入口）、`agents/qa/nodes.py`（四节点 + 工具结果摘要 + 参数兜底）。

## 3. 数据源说明

| 数据源 | 内容 | 现状（2026-08-14） |
|---|---|---|
| 评课库 `data/course_data.db` | icourse.club 真实评课 + jw 官方培养方案 | 632 个培养方案、2351 门课程、44403 条评论、2981 位教师、45168 条方案-课程关联 |
| 个人库 `database/xiaowo.db` | CAS 登录后拉取的本地缓存 + 日程 | 测试账号 PB25111691：26 条成绩、14 门选课 |
| jw 教务 API（CAS 登录态） | 成绩/课表/考试/个人方案树实时数据 | 测试版强制走 SQLite 降级，避免真实请求超时 |
| 测试备份 `%TEMP%\xiaowo_personal\data.json` | 个人数据爬取备份（含方案树） | 测试版数据来源；正式版不受影响 |

## 4. 功能清单

| 模块 | 功能 | 状态 |
|---|---|---|
| 📚 智能问答 | 50 篇知识库 RAG（办事/教务/生活/就业/科研升学） | ✅ 完成 |
| 📊 课业助手 | 成绩查询、课表查询、GPA 计算、考试安排、空教室 | ✅ 完成 |
| 🔍 选课顾问 | 按培养方案分组推荐（必修组+选修组）、已修排除、教师分析、课程对比 | ✅ 完成（本轮修复后实测通过） |
| 📅 日程管理 | 日程增删查、冲突检测、课表导入、空闲匹配 | ✅ 完成 |
| 🎓 培养方案 | 方案总览/学期规划/培养进度三合一页面（`ui/program_page.py`） | ✅ 完成 |
| 🎪 活动推荐 | 校团委活动 + 课表空闲 MMR 推荐（每日一次弹窗） | ✅ 完成 |
| 🧪 测试版 | `app_test.py` 模拟登录 + 个人数据注入（env 门控外的独立入口） | ✅ 本轮新增 |

## 5. 本轮修复记录（2026-08-14）

提交：`b02bc19`（QA 多轮记忆）、`2ff8e17`（选课推荐按培养方案修复 + 测试版入口）。

| 问题（用户反馈） | 根因 | 修复 |
|---|---|---|
| 推荐没按培养方案、乱推 | 方案定位用 `college LIKE` 模糊匹配，专业名"人工智能"误定位到"数据科学与大数据技术"方案 | `_resolve_program`/`_program_hint` 改为方案名称精确匹配优先，college 仅回退 |
| 测试版数据不完整（成绩只显示 5 门） | `_build_tool_summary` 通用分支 `json.dumps(...)[:800]` 截断，LLM 只见部分数据 | 9 类工具改为按 60/80 条上限完整 dump（覆盖当前 26 门/14 门，成绩/课表/进度/推荐） |
| 知道大二仍乱推 | `get_program_progress` 缺 `taken_courses` 时把已修课程误判为必修缺口（"已修 0"） | `_enrich_program_args`/`_enrich_recommend_args` 兜底注入：专业/年级/已修课程/个人方案树 |
| 推荐无学分信息 | 推荐结果带 credit 但 dump 未展示 | dump 补学分展示 |
| LLM 把评课库历史学期当"下学期" | 学期语义混淆 | recommend_courses dump 增加选课季说明行 + "近3次开课/方案学期"语义标注 |
| 多轮对话无记忆 | run_qa 未接收历史 | 最近 20 条对话历史传入决策链路 |

## 6. 开发经验总结（本轮新沉淀）

### 6.1 实测闭环流程（项目测试的统一方法论）

1. 启动测试实例：`python -m streamlit run app_test.py --server.port 8501`（测试期常驻，任务收尾停止）
2. Playwright 模拟用户提问，抓取真实回答文本与截图（Edge persistent context）
3. 判断回答质量：把回答中的课程与培养方案白名单（course_data.db 方案课程）比对，检查已修课程残留
4. 定位根因 → 修改代码 → `py -3 -m py_compile` 编译 → 强制重启实例 → 重跑实测确认

**分工约定**：起停实例、跑 Playwright 脚本、截图等机械执行派给 Claude Code / Codex CLI 双主力轮流；观察输出、诊断与改代码由指挥中心（Qoder 主会话）保留。

### 6.2 环境要点（Windows）

- 用 `py -3` 而非 `python`：`python` 是 WindowsApps 占位符，静默无输出
- PowerShell 不支持 `&&`，用 `;` 分隔
- 内联 `-c` 引号转义频繁失败：**写脚本文件后运行**，最稳
- Streamlit 修改 agent 代码后 Rerun 不生效：必须 `taskkill` 杀 8501 端口进程后重新启动（taskkill 可能需提权）
- 启动实例用完整解释器路径 `C:\Users\Richelieu\AppData\Local\Programs\Python\Python314\python.exe`，避免占位符问题

### 6.3 常见坑位与修复模式

| 坑位 | 修复模式 |
|---|---|
| 方案定位误伤（专业名模糊匹配串库） | 名称精确匹配优先，模糊字段仅做回退 |
| LLM 决策缺参导致工具退化成全量乱推 | 在 act 层兜底注入参数（专业/年级/已修课程/个人方案树），不依赖 LLM 传参 |
| 工具结果截断导致回答数据不完整 | 按 60/80 条上限完整 dump，按工具类型定制展示格式 |
| LLM 输出脏前缀（smart_toy 等对话痕迹） | 按用户要求保留不剥离；如将来需清理，注意真实输出分隔符是换行而非竖线（实测脚本把换行 replace 成竖线会误导判断） |
| 实测脚本误判 | 合规提示语（"以综合教务系统为准"）会被课程提取脚本误判为推荐课程，验收时须人工甄别 |
| 查实例是否加载新代码 | 怀疑代码未生效时在关键路径加日志（重定向到文件），比对服务端日志与 UI 显示定位偏差层 |

### 6.4 指挥中心分工模式

开发与测试任务统一走指挥中心模式：任务拆块 → 编写开发文档（目标/文件范围/验收标准）→ 分发 Claude Code CLI（`claude.exe -p` + accept_edits）与 Codex CLI 双主力分批并行（每次 2-3 个独立模块）→ 收集产出逐项核对修正 → git 提交由指挥中心统一执行。流水线验收用 `python -m scripts.dev_pipeline run "任务名" --executor qoder`。

注意：Codex 曾有持续 429 限流问题，派发时优先 Claude Code、Codex 作备选；git commit 需 `-c user.name="xiaowo-dev" -c user.email="xiaowo@local"` 显式指定身份。

## 7. 已知问题与限制

- 真实登录路线（CAS 登录页拒绝 service 参数）未打通，个人数据靠测试备份 + SQLite 降级验证；部署需配置 CAS service 白名单
- 评课数据为 icourse.club 抓取快照，新开课程可能暂无评论；样本量过少的课程/教师评分仅供参考
- 教师对比样本量差异大（1~553 条），均分排序存在小样本偏差
- 课程推荐依赖科大 LLM 平台，平台不可用时无法出推荐
- 回答开头偶发 "smart_toy" 脏前缀（deepseek 输出痕迹），按用户要求保留
- `scripts/tmp_*.py` 为调试临时脚本，未纳入版本库，可随时清理

## 8. 后续改进建议

1. **部署打通真实登录**：配置 CAS service 白名单后验证真实数据链路（成绩/课表/方案树实时拉取）
2. **测试脚本沉淀**：把本轮 Playwright 实测脚本整理为正式回归测试（scripts/ 下固定命名），纳入 dev_pipeline 流水线
3. **培养方案数据更新**：jw 官方方案爬取脚本定期重跑，保持 course_data.db 方案与教务一致
4. **推荐个性化增强**：结合 GPA/已修课程结构做进阶画像（如冲分/硬核/均衡策略的深度应用）
5. **前端体验**：培养方案页与选课顾问联动（点击方案课程直接发起推荐），减少手动提问

---

> 快速开始、验收命令与项目结构详见 [README.md](README.md)；Tool 规格见 [docs/tool-specs.md](docs/tool-specs.md)；开发记录见 [docs/dev-log/](docs/dev-log/)。
