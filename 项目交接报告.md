# 🐌 小蜗 — 项目交接报告（Agent 能力继承包）

> 生成时间：2026年7月31日
> 用途：将本报告完整提交给下一个 Agent，即可零成本接手本项目的全部工作与能力。
> 报告结构：第一～九章 = 项目现状总结；附录 A～F = 前一个 Agent 的技能与知识继承包。

---

## 一、项目概况

**小蜗**是中国科学技术大学（USTC）的校园 AI 智能助手，通过自然语言对话替代在十几个校园系统之间来回切换。

- **一句话定位**：说一句话，AI 帮你跨系统获取、分析、整合校园信息。
- **架构版本**：v2.1（Plan-and-Execute），已支持 CAS 统一身份认证登录。
- **运行方式**：`streamlit run app.py`，本地访问 http://localhost:8501

---

## 二、整体架构（v2.1 Plan-and-Execute）

```
用户输入
   │
   ▼
Router（agents/router.py）—— 关键词快速匹配 + LLM 双层路由 + 复杂度判断
   │
   ├── simple 查询 ──▶ 子 Agent（faq/course/advisor/schedule）──▶ Tool ──▶ 回答
   │
   └── complex 查询 ──▶ Planner（生成≤5步计划，JSON格式）
                            │
                            ▼
                        validate_plan（计划校验，失败降级 FAQ）
                            │
                            ▼
                        Executor（逐步执行，支持 {{step_N.field}} 依赖占位）
                            │
                            ▼
                        综合回答
```

关键机制：
- **Context**（`agents/context.py`）：会话级上下文，跨多次查询保持，存储对话历史与中间结果。
- **ServiceContainer**（`services/service_container.py`）：单例依赖注入容器，集中管理 DatabaseManager / FAQVectorStore / CASClient / CatalogAPI。
- **数据分层**：公共数据（FAQ、空教室、考试安排、课程搜索）无需登录；个人数据（课表、成绩、选课结果、培养方案）需 CAS 登录后经 jw 内部 API 获取并缓存到 SQLite。
- **身份贯通**：`st.session_state["user"]` 存储登录用户；未登录时降级到演示账号 `PB20240001`。

---

## 三、四大业务模块

| 模块 | 核心能力 | 数据来源 | 关键文件 |
|------|---------|---------|---------|
| 📚 智能问答 | RAG 检索 + LLM 生成，回答校园 FAQ | ChromaDB（50 篇 Markdown 文档，5 分类） | `agents/faq_agent.py` + `tools/faq_tools.py` |
| 📊 课业助手 | 课表/成绩/GPA/空教室/考试 | SQLite 缓存 + catalog 公开 API + jw 内部 API（登录后） | `agents/course_agent.py` + `tools/course_tools.py` |
| 🔍 选课顾问 | 多轮偏好收集 + 三维度加权推荐 + 课程对比 + 教师分析 | SQLite（评课/教师评价表） | `agents/advisor_agent.py` + `tools/advisor_tools.py` |
| 📅 日程管理 | 自然语言添加日程 + 冲突检测 + 课表导入 | SQLite（events 表）+ `utils/time_parser.py` | `agents/schedule_agent.py` + `tools/schedule_tools.py` |

**Tool 清单（20 个）**：
`search_faq`, `get_faq_categories`, `query_schedule`, `find_empty_room`, `query_grade`, `calc_gpa`, `query_exam`, `collect_preferences`, `recommend_courses`, `compare_courses`, `analyze_teacher`, `add_event`, `get_day_view`, `get_week_view`, `check_conflict`, `import_schedule`, `search_courses`, `get_semester_list`, `query_course_selection`, `query_program`

---

## 四、数据层

### SQLite（`database/xiaowo.db`）
8 张表（`database/schema.sql`）：`courses`、`student_courses`、`student_grades`、`course_reviews`、`teacher_reviews`、`events`、`reminders`、`chat_history`

### 种子数据（`database/seed_data.py`）
课程/课表/考试基于真实 catalog API 抓取（2026 春季学期）；成绩与评课为登录前演示数据，登录后由 jw API 实时拉取真实数据。
| 数据 | 数量 | 说明 |
|------|------|------|
| 真实课程 | 8 门 | 数学分析(B2)、线性代数(B1)、数据结构A、Python程序设计、大学物理实验、基础英语II、操作系统原理与设计(H)、概率论进阶 |
| 演示成绩 | 6 条 | 演示学生 PB20240001 |
| 评课数据 | 8 门 | 评分/难度/工作量/标签/摘要 |
| 教师评价 | 3 位 | 教学风格/优缺点/摘要 |
| 日程事件 | 3 个 | 演示用 |

### 向量知识库（`knowledge/chroma_db`）
50 篇 Markdown FAQ，5 分类：办事 10 / 就业 5 / 教务 15 / 生活 13 / 科研与升学 7。Embedding 采用三层降级：校内 API（`qwen3-embedding`，实测可用，4096 维）→ 本地 `shibing624/text2vec-base-chinese` → 关键词 n-gram 哈希 fallback。

---

## 五、外部系统对接现状（核心资产）

### 1. catalog.ustc.edu.cn —— 公开 API（17 个，全部无需认证）
封装在 `tools/api_client.py`（CatalogAPI 类，5 分钟内存缓存，错误返回 `{"error": ...}` 不抛异常）。

| 端点 | 用途 |
|------|------|
| `GET /api/teach/timetable-public-all/{date}` | 某天全校教室占用（空教室查询的真实数据源） |
| `GET /api/teach/semester/list` | 学期列表 |
| `GET /api/teach/lesson/list-for-teach/{semId}` | 某学期全部开课 |
| `POST /api/teach/lesson/infos` | 课程详情（body: codes+semester） |
| `GET /api/teach/exam/list/{semId}` | 专业课考试安排 |
| `GET /api/teach/general-exam/list/{semId}` | 通修课考试安排（需与上者合并） |
| `GET /api/teach/course/search?keyword=` | 课程搜索 |
| `POST /api/teach/course/infos` | 课程详情批量查询 |
| `GET /api/teach/course/department/{deptId}` | 按院系查课程 |
| `GET /api/teach/department/college-tree` | 院系结构树 |
| `GET /api/teach/program/tree` / `info/{id}` | 培养方案 |
| `GET /api/teach/major/list/{deptId}` | 专业列表 |

**当前学期 ID**：`421`=2026春季，`461`=2026秋季，`441`=2026夏季（注意：学期列表按 id 升序，id=1 是 2002 年的老学期，取当前学期要从列表末尾找）。

**教学楼代码映射**（已内置于 `api_client.py`）：1/2/3/5=一二三五教，8=体育馆，9=艺术中心，11/12/13=高新图书教育中心A/B/C楼，14=师生活动中心，15/16=学科楼，22=信智楼，41/42/43=太湖路校区。

### 2. jw.ustc.edu.cn —— 教务内部 API（需 CAS 登录后的 Cookie）
封装在 `services/cas_client.py`：

| 端点 | 用途 |
|------|------|
| `GET /for-std/course-table/get-data?bizTypeId=2&semesterId={}&dataId={}` | 个人课表 |
| `GET /for-std/grade/sheet/getGradeList?trainTypeId=1&semesterIds={}` | 成绩 |
| `GET /for-std/grade/sheet/getSemesters` | 成绩学期列表 |
| `GET /for-std/course-take-query/semester/{semId}/search` | 选课结果 |
| `GET /for-std/exam-arrange/info/{studentDataId}` | 个人考试安排 |
| `GET /for-std/student-info/info/{studentDataId}` | 学生信息 |
| `GET /for-std/program/root-module-json/{moduleId}` | 培养方案 |
| `GET /home/menu` | 首页菜单（用于确认 session 有效、提取 dataId） |
| `GET /home/get-current-teach-week` | 当前教学周 |

关键参数：`bizTypeId=2`=本科；`studentDataId` 是学生内部数字 ID（非学号），从 `/home/menu` 页面 HTML 正则提取（`dataId["\s:=]+(\d+)`）。

### 3. CAS 统一身份认证（id.ustc.edu.cn）
- **登录方式为 CAS 重定向跳转**（UI 上只有一个"🔑 科大统一身份认证登录"按钮，无表单登录）：
  1. 点击按钮跳转 `https://id.ustc.edu.cn/cas/login?service={CAS_SERVICE_URL}`
  2. 用户在科大官方页面完成 密码 + 短信二次验证（2FA）
  3. CAS 回调携带 `?ticket=ST-xxx`，`ui/chat.py` 的 `check_cas_callback()` 捕获
  4. `cas_client.validate_ticket()` 调用 `/cas/serviceValidate` 解析 XML 中的 `<cas:user>`
  5. 访问 jw `/home/menu` 建立 Cookie session → 提取 student_data_id → 登录完成
- **为什么不用表单登录**：科大 CAS 有短信二次验证（2FA），纯账号密码的表单模拟无法完成；代码中 `login()` 表单方式仍保留但仅作备用。
- **⚠️ 关键限制**：`CAS_SERVICE_URL`（默认 `http://localhost:8501`）必须向学校 IT 部门申请注册到 CAS service 白名单，否则返回 "service not authorized"。部署到 `*.ustc.edu.cn` 域名后可能被自动放行。
- Session 有效期 UI 层按 1 小时提示过期（`_render_login_panel()`）。

---

## 六、技术栈

| 层次 | 技术 |
|------|------|
| 语言 | Python 3.14 |
| Agent 框架 | LangChain 1.3.14（`create_agent` 新 API） |
| LLM | 校内 OpenAI 兼容 API（`https://api.llm.ustc.edu.cn/v1`，模型 `deepseek-chat`，密钥经环境变量注入，见附录 B） |
| 前端 | Streamlit |
| 数据库 | SQLite（标准库） |
| 向量库 | ChromaDB + sentence-transformers（三层降级） |
| 网络/加密 | requests + pycryptodome（CAS RSA 加密） |

---

## 七、目录结构（当前真实状态）

```
小蜗/
├── app.py                     # Streamlit 主入口（v2.1，含 CAS 回调检查）
├── config.py                  # 配置（LLM/Embedding/DB/CAS_SERVICE_URL/DEMO_STUDENT）
├── requirements.txt
├── init_check.py
├── agents/
│   ├── router.py              # 双层路由 + 复杂度判断
│   ├── planner.py             # 全局 Planner（PlanStep/Plan 数据类 + JSON 计划）
│   ├── executor.py            # Executor（工具注册表 + 依赖占位替换）
│   ├── context.py             # 会话级 Context
│   ├── factory.py             # build_agent / invoke_agent
│   └── faq/course/advisor/schedule_agent.py
├── services/
│   ├── cas_client.py          # CAS 客户端（重定向+表单双模式，jw API 封装）
│   └── service_container.py   # 单例依赖容器
├── tools/
│   ├── api_client.py          # CatalogAPI + 教学楼映射
│   ├── faq_tools.py / course_tools.py / advisor_tools.py / schedule_tools.py
├── knowledge/                 # ChromaDB + 50 篇 FAQ Markdown
├── database/                  # schema.sql / db_manager.py / seed_data.py / xiaowo.db
├── ui/chat.py                 # 侧边栏登录面板 + 对话区 + CAS 回调
├── utils/                     # llm_client / time_parser / gpa_calculator / logger
└── scripts/                   # 一次性脚本：crawl_data.py / crawl_v2.py /
                               #   gen_seed_sql.py / gen_seed_v2.py / check_data.py
```

---

## 八、已完成的里程碑

1. **v1.0**：四大模块 + 路由 + 16 Tool + 种子数据，高保真 Demo。
2. **A 阶段（架构升级）**：Plan-and-Execute（Planner/Executor/Context），Tool 扩充到 20 个。
3. **B 阶段（真实数据）**：
   - B1：空教室 + 考试安排接入 catalog 公开 API ✅
   - B2：课程搜索增强（`search_courses` / `get_semester_list`）✅
   - B3：CAS 登录模块（重定向 + ticket 验证）✅
   - B4/B5：个人课表/成绩/选课/培养方案（jw API 封装完成，待真实登录验证）
4. **登录贯通方案（C1-C5）**：侧边栏登录面板、student_id 身份贯通、session 生命周期、数据分层、未登录体验优化。
5. **最终形态**：仅保留 CAS 跳转登录；种子数据中课程/课表/考试基于真实 catalog API 抓取，成绩与评课为登录前演示数据。

---

## 九、已知限制与待办

| 事项 | 状态/说明 |
|------|----------|
| CAS service 白名单 | ❌ 未申请。需向学校 IT 注册回调 URL，否则重定向登录被拒 |
| jw 个人数据端到端验证 | 代码已完成，但需真实登录（含 2FA）后验证课表/成绩同步 |
| 评课数据覆盖率 | 种子数据仅 8 门课，真实评课社区覆盖率低 |
| Embedding | 校内 API（`qwen3-embedding`）实测可用（4096 维）；本地 text2vec 与关键词 fallback 为降级备用 |
| 提醒推送 | 仅应用内，无系统通知 |
| 下一步建议 | ①申请 CAS 白名单 → ②端到端登录测试 → ③部署到 ustc.edu.cn 域名 → ④评课数据扩充 |

---
---

# 附录：Agent 能力继承包

> 以下内容为前任 Agent 的全部工作知识与技能。继任 Agent 阅读后即具备同等能力。

## 附录 A：grilling Skill（原文）

用户通过 `/grilling` 命令触发的压力测试技能。存放位置：`C:\Users\Richelieu\.qoder\skills\grilling\SKILL.md`

```markdown
---
name: grilling
description: Grill the user relentlessly about a plan, decision, or idea. Use when the user wants to stress-test their thinking, or uses any 'grill' trigger phrases.
---

Interview me relentlessly about every aspect of this until we reach a shared understanding. Walk down each branch of the decision tree, resolving dependencies between decisions one-by-one. For each question, provide your recommended answer.

Ask the questions one at a time, waiting for feedback on each question before continuing. Asking multiple questions at once is bewildering.

If a *fact* can be found by exploring the environment (filesystem, tools, etc.), look it up rather than asking me. The *decisions*, though, are mine — put each one to me and wait for my answer.

Do not act on it until I confirm we have reached a shared understanding.
```

**执行要点**：一次只问一个问题；每个问题附推荐答案；事实自己查环境，决策交给用户；达成共识前不动手。

## 附录 B：环境配置速查

| 项 | 值 |
|----|----|
| Python 解释器 | `C:\Users\Richelieu\AppData\Local\Programs\Python\Python314\python.exe` |
| 工作区 | `f:\小蜗` |
| 启动命令 | `streamlit run app.py`（http://localhost:8501，支持热重载） |
| 初始化检查 | `python init_check.py` |
| LLM API | `https://api.llm.ustc.edu.cn/v1`（注意不是 llm/api/v1），模型 `deepseek-chat`（经 `.env` 的 `LLM_MODEL` 配置） |
| LLM 密钥 | 经环境变量 `LLM_API_KEY` 注入，见 `.env`（不入库） |
| 操作系统 | Windows 25H2，Shell 为 PowerShell v1.0 |

**PowerShell 注意事项（重要）**：
- 不支持 `&&`，用 `;` 分隔命令
- `$_`、`$env:` 等 `$` 变量在命令行转义时经常被吞掉，尽量避免在 Where-Object 中使用，改用其他工具（如 Grep/Glob）
- 不支持 `2>nul` 这类 CMD 重定向语法
- 禁止使用 `cmd /c`，终端只接受 PowerShell 命令

## 附录 C：踩坑记录（血泪教训）

1. **LangChain 1.3.14 API 变更**：`create_tool_calling_agent` 和 `AgentExecutor` 已被移除。必须用 `from langchain.agents import create_agent`，返回 `CompiledStateGraph`，调用格式为 `{"messages": [HumanMessage(content=...)]}` 而非 `{"input": ...}`。`agents/factory.py` 已适配。
2. **Stale pycache**：修改代码后 Streamlit 可能仍用旧字节码，出现 `ImportError`（如 `check_cas_callback` 找不到）。解法：杀掉所有 Python 进程 + 删除对应 `__pycache__/*.pyc` 再重启。
3. **学期 ID 陷阱**：`/api/teach/semester/list` 按 id 升序返回，id=1 是 2002 年的学期。当前学期要从列表末尾取（421=2026春，461=2026秋，441=2026夏）。
4. **种子 SQL 换行符**：catalog API 的 `dateTimePlacePersonText` 字段含 `\n`，直接拼进 SQL 会导致解析错误，必须 `.replace('\n', ' ')`。
5. **CAS 表单登录死路**：科大 CAS 有短信 2FA，表单模拟无法完成。唯一可行路径是 CAS 重定向 + 服务端 ticket 验证。
6. **CAS service 未授权**：回调 URL 未在学校 IT 白名单中会被 CAS 拒绝（"service not authorized"），这是部署前必须解决的阻塞项。
7. **课程列表前 50 门偏物理**：用 `list-for-teach` 取种子数据时，只取前 N 门会集中在某个院系，要拉全量（约 2520 门）再按关键词筛选。
8. **LLM 端点与模型（2026-08-05 实测）**：`api.llm.ustc.edu.cn/v1` + `deepseek-chat` 可用（实际路由至 `deepseek-v4-flash-ascend`，推理类模型，`max_tokens` 过小时输出可能全部进入 `reasoning_content` 导致 `content` 为空）；`qwen3.6-chat` 亦实测可用。旧端点 `llm.ustc.edu.cn/api/v1` 已失效。

## 附录 D：开发约定

- **依赖注入**：所有全局依赖（DB/向量库/CAS/CatalogAPI）通过 `ServiceContainer` 单例获取，禁止全局变量 + `set_xxx()`。
- **错误处理**：API 客户端层返回 `{"error": "..."}` 而不是抛异常；Tool 层负责把 error 翻译成用户友好提示。
- **缓存策略**：CatalogAPI 5 分钟内存缓存；登录用户数据同步到 SQLite 作为缓存层。
- **数据分层**：公共数据永远可查；个人数据未登录时返回锁定提示并引导登录（`_is_locked()`）。
- **隐私**：拒绝查询他人成绩/课表（如"帮我查张三的成绩"）。
- **Planner 唯一性**：复杂查询统一走全局 Planner，不在子 Agent 内部做规划。
- **代码修改后**：先语法检查，再依赖 Streamlit 热重载；涉及模块导入变更时重启进程。

## 附录 E：关键文件入口速查

| 想做的事 | 看哪里 |
|---------|--------|
| 改登录 UI | `ui/chat.py`（`_render_login_panel` / `check_cas_callback` / `_logout`） |
| 改认证逻辑 | `services/cas_client.py`（`get_login_url` / `validate_ticket` / `login_with_ticket`） |
| 改登录编排 | `services/service_container.py`（`login_with_ticket`） |
| 改查询主流程 | `app.py`（`process_query` / `_process_complex`） |
| 加新 Tool | 对应 `tools/*_tools.py` + `agents/planner.py` 的工具清单表 + `agents/executor.py` 注册表 |
| 改路由 | `agents/router.py` |
| 改种子数据 | `database/seed_data.py`（参考 `scripts/gen_seed_v2.py` 重新生成） |
| 改配置 | `config.py`（`CAS_SERVICE_URL` 部署前必改） |

## 附录 F：继任 Agent 行动建议（按优先级）

1. **向学校 IT 申请 CAS service 白名单**（阻塞项），或先部署到 `*.ustc.edu.cn` 域名试试自动放行。
2. 白名单通过后，走一遍完整登录链路：跳转 → 密码 → 短信 2FA → ticket 回调 → jw session → 课表/成绩同步到 SQLite，验证 B4/B5 端到端。
3. 若 `validate_ticket` 拿到的 `<cas:user>` 与学号格式不符，需要调整 `_extract_data_id` 的解析。
4. 扩充评课数据覆盖率（当前 8 门）。
5. 考虑 session 真实过期检测（目前只是 UI 层 1 小时计时）。

---

*报告版本：v1.1（2026-08-05 修订：知识库数量/模型名/种子数据来源如实化，删除明文密钥） | 前任 Agent 已完成全部 C1-C5 登录贯通，系统当前可在 localhost:8501 正常运行*
